Package 7 - Part 5

Run the SQL in database/schema.sql.

Add these routes to backend/app.js:

app.use(
    "/api/fee-plans",
    require("./routes/feePlanRoutes")
);

app.use(
    "/api/scholarship-applications",
    require("./routes/scholarshipApplicationRoutes")
);

This part adds:
- Installment plans
- Installment viewing
- Scholarship applications
- Scholarship application history

The reminder tables are included now so the next finance-notification
part can process them safely.
