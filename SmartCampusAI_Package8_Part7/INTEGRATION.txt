PACKAGE 8 - PART 7

Run database/schema.sql.

Add to backend/app.js:

app.use(
    "/api/notification-templates",
    require("./routes/notificationTemplateRoutes")
);

app.use(
    "/api/bulk-notifications",
    require("./routes/bulkNotificationRoutes")
);

Adds:
- Reusable notification templates
- {{variable}} interpolation
- Admin template management
- Bulk notification sending
- Template-based notification architecture
- Final Package 8 integration building blocks

Example template:

templateKey:
attendance_warning

titleTemplate:
Attendance Alert for {{studentName}}

messageTemplate:
Your attendance is currently {{percentage}}%.

A future module can call notificationTemplateService.build()
with variables such as:

{
    studentName: "Student Name",
    percentage: "68"
}

Bulk notification endpoint:

POST /api/bulk-notifications

Example JSON:

{
    "recipientUserIds": [101,102,103],
    "title": "Important Campus Notice",
    "message": "Please check the student portal.",
    "notificationType": "Announcement",
    "priority": "High"
}

Package 8 is now functionally complete at the application layer:
notifications, announcements, preferences, delivery architecture,
automatic alerts, scheduling, monitoring, templates and bulk sending.
